#include <stdio.h>

int main() {
    int a, b ,h;
    scanf("%d %d %d", &a, &b, &h);
    
    int days = (h-a) / (a-b);
    if((h-a)%(a-b)!=0) {
        days++;
    }
    days++;
    
    printf("%d\n", days);
    return 0;
}
